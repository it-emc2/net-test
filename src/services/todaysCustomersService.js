import { bitrixCall } from "./bitrixClient.js";

// export async function getKundendatenForStage(stageId) { ... }
export async function getKundendatenForStage(stageId = "C72:UC_YOESDE") {
  // paste your logic here, replacing direct fetch calls with bitrixCall(...)
  // listDealsByStage, getContact, listRequisitesByContact, listAddressesByRequisiteId, etc.
  return [];
}