# Copy to .env.local and fill in real values
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/
MONGODB_DB=KonfiguratorDB
NODE_ENV=production
# PORT is provided by the host in production; for local docker 'PORT=3000' is fine
